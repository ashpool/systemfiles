function add_pythonpath {
  local dir=$1
  if [ -d $dir ]; then
    PYTHONPATH="$dir:${PYTHONPATH}"
  fi
}

python_paths=$(find $HOME/prjs/spotify -type f -maxdepth 2 -name 'setup.py')
for python_path in $python_paths; do
  add_pythonpath $(dirname $python_path) 
done
